import QtQuick 2.0
import "."

Text {
    anchors.topMargin: Style.dp(55)

    color: Style.inactiveColor
    font.pixelSize: Style.fontSizeBig
    font.family: "Didot"

    renderType: Text.NativeRendering
}
